import { DetalhesJogoPlayer } from '@/components/Player/DetalhesJogoPlayer';

/** TELA 15 - Detalhes do jogo (jogador). */
export default function PlayerGameDetailPage() {
  return <DetalhesJogoPlayer />;
}
